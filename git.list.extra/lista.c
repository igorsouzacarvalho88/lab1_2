#include <stdio.h>
#define MTAMLISTA 10
/ / Arquivo: myboolean.h 
# ifndef MYBOOLEAN_H
 # define MYBOOLEAN_H

# Define  falsos  0 
# define  verdade  um 
typedef  int  bool ;  / / ou # define bool int

# Endif

typedef struct{
	int chave[MTAMLISTA];
	int  nelement;
	int x;
	} listaint;

 bool consulta(listaint l ,int i)	
 { 
	 
			int i;
	 for((i=0;) i<l.nelement;i++) 
	 
	 		if(l.chave[i]==x);
			return true;
	 
	 
	 
	 return false;
	
	
}

